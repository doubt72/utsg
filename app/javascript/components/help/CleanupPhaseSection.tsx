import React from "react";

export default function CleanupPhaseSection() {
  return (
    <div>
      <p>
        Several things happen during the prep phase, the first is close combat resolution, then
        overstacked units are removed, then counter status markers are updated, then smoke
        dispersion is checked, then fire spreading or extinguishing are checked, then variable
        weather is checked.
      </p>
    </div>
  );
}
